#!/usr/bin/env perl
use warnings;
use strict; 
use List::Util qw(max);

sub complementDNA
{
    my @seq = @_;
    return @seq;
}

# my $result = complementDNA('aaa');
# print $result;

my $s = 'aaa';
print $s;